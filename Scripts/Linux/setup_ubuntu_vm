#!/bin/bash

# Description: This script configures specific things for my virtual machines.
#
# Usage: setup_ubuntu_vm
#
# Written by Göran Gustafsson (gustafsson.g at gmail.com).
# License: BSD 3-Clause.

sudo apt install \
  apache2 \
  bind9 \
  cuetools \
  dia \
  gnupg2 \
  gnuplot \
  gparted \
  hping3 \
  ncftp \
  netperf \
  nfs-kernel-server \
  nginx-full \
  open-vm-tools \
  open-vm-tools-desktop \
  php5 \
  r-base \
  samba \
  shntool \
  sslstrip \
  tftp \
  tftpd \
  tshark \
  wireshark

sudo dpkg-reconfigure wireshark-common
sudo usermod -a -G dialout,wireshark $USER

sudo systemctl disable apache2 bind9 netperf nfs-kernel-server nginx smbd
sudo systemctl stop    apache2 bind9 netperf nfs-kernel-server nginx smbd
