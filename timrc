# vim: ft=zsh

if [[ $OSTYPE == darwin* ]]; then
  #timer_cmd=
  #timer_repeat_cmd=

  #interval=

  work_cmd="terminal-notifier -message 'Start working now!' -title Tim -activate com.apple.Terminal >& /dev/null && say start working now"
  break_cmd="terminal-notifier -message 'Take a little break!' -title Tim -activate com.apple.Terminal >& /dev/null && say take a little break"

  pomodoro_cmd="say working session is over. you win && open ~/Storage/Celebrate.flv"
  #pomodoro_repeat_cmd=

  #pomodoro_work=
  #pomodoro_break=
  #pomodoro_stop=
else
  #timer_cmd=
  #timer_repeat_cmd=

  #interval=

  #work_cmd=
  #break_cmd=

  #pomodoro_cmd=
  #pomodoro_repeat_cmd=

  #pomodoro_work=
  #pomodoro_break=
  #pomodoro_stop=
fi
